@model WebBanGiayAdidas.Models.Product

@{
    ViewData["Title"] = "Create";
    Layout = "~/Areas/Admin/Views/Shared/_Admin.cshtml";
}
@section naviheader {
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a asp-area="Admin" asp-controller="Dashboard" asp-action="Index" class="nav-link">Home</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a asp-area="Admin" asp-controller="Products" asp-action="Index" class="nav-link">Sản phẩm</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a asp-area="Admin" asp-controller="Products" asp-action="Create" class="nav-link">Thêm mới</a>
        </li>
    </ul>
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Thêm mới sản phẩm</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
@*                     <li class="breadcrumb-item"><a asp-area="Admin" asp-controller="Categories" asp-action="Index">Danh mục</a></li>
                    <li class="breadcrumb-item active">Thêm mới</li> *@
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">

    <!-- Default box -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Thông tin sản phẩm</h3>
        </div>
        <div class="card-body">
            <form asp-action="Create" enctype="multipart/form-data">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="card-header p-2">
                    <ul class="nav nav-pills">
                        <li class="nav-item">
                            <a class="nav-link active" href="#activity" data-toggle="tab">Thông tin chung</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#timeline" data-toggle="tab">Hình ảnh</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#settings" data-toggle="tab">SEO</a>
                        </li>
                    </ul>
                </div>

                <div class="tab-content p-3" id="tabContent">
                    <!-- Tab 1: Thông tin chung -->
                    <div class="tab-pane fade show active" id="activity">
                        <div class="form-group">
                            <label asp-for="Title" class="control-label">Tên sản phẩm</label>
                            <input asp-for="Title" class="form-control" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="ProductCategoryId" class="control-label">Danh mục sản phẩm</label>
                            <select asp-for="ProductCategoryId" class="form-control" asp-items="ViewBag.ProductCategoryId"></select>
                        </div>
                        <div class="form-group">
                            <label asp-for="ProductCode" class="control-label">Mã sản phẩm</label>
                            <input asp-for="ProductCode" class="form-control" />
                            <span asp-validation-for="ProductCode" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Description" class="control-label">Mô tả</label>
                            <input asp-for="Description" class="form-control" />
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="Detail" class="control-label">Chi tiết</label>
                            <textarea asp-for="Detail" class="form-control" id="txtDetail"></textarea>
                            <span asp-validation-for="Detail" class="text-danger"></span>
                        </div>

                        <div class="row">
                            <div class="col-4">
                                <div class="form-group">
                                    <label asp-for="Price" class="control-label">Giá</label>
                                    <input asp-for="Price" class="form-control" />
                                    <span asp-validation-for="Price" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-4">
                                <div class="form-group">
                                    <label asp-for="PriceSale" class="control-label">Giá khuyến mãi</label>
                                    <input asp-for="PriceSale" class="form-control" />
                                    <span asp-validation-for="PriceSale" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-4">
                                <div class="form-group">
                                    <label asp-for="Quantity" class="control-label">Số lượng</label>
                                    <input asp-for="Quantity" class="form-control" />
                                    <span asp-validation-for="Quantity" class="text-danger"></span>
                                </div>
                            </div>

                            <div class="col-3">
                                <div class="form-group form-check">
                                    <input class="form-check-input" asp-for="IsActive" />
                                    <label class="form-check-label" asp-for="IsActive">Trạng thái</label>
                                </div>
                            </div>

                            <div class="col-3">
                                <div class="form-group form-check">
                                    <input class="form-check-input" asp-for="IsHot" />
                                    <label class="form-check-label" asp-for="IsHot">Hot</label>
                                </div>
                            </div>
                            <div class="col-3">
                                <div class="form-group form-check">
                                    <input class="form-check-input" asp-for="IsFeature" />
                                    <label class="form-check-label" asp-for="IsFeature">Phổ biến</label>
                                </div>
                            </div>
                            <div class="col-3">
                                <div class="form-group form-check">
                                    <input class="form-check-input" asp-for="IsSale" />
                                    <label class="form-check-label" asp-for="IsSale">Khuyến mãi</label>
                                </div>
                            </div>
                        </div>

                        <div class="form-group">
                            <label asp-for="CreatedDate" class="control-label">Ngày tạo</label>
                            <input asp-for="CreatedDate" class="form-control" />
                            <span asp-validation-for="CreatedDate" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="CreatedBy" class="control-label">Người tạo</label>
                            <input asp-for="CreatedBy" class="form-control" />
                            <span asp-validation-for="CreatedBy" class="text-danger"></span>
                        </div>
                    </div>

                    <!-- Tab 2: Hình ảnh -->
                    <div class="tab-pane fade" id="timeline">
                        <div class="form-group">
                            <label asp-for="Image" class="control-label">Ảnh đại diện</label>
                            <div class="input-group">
                                <div class="custom-file">
                                    <input asp-for="Image" type="file" class="custom-file-input" id="imageUpload" name="imageFile" accept="image/*">
                                    <label class="custom-file-label" for="imageUpload">Chọn ảnh</label>
                                </div>
                            </div>
                            <span asp-validation-for="Image" class="text-danger"></span>
                        </div>
                        <img id="previewImage" src="#" alt="Ảnh xem trước" style="max-width: 200px; display: none; margin-top: 10px; margin-bottom: 20px;" />
@*                         <div class="form-group">
                            <label>Ảnh mô tả (nhiều ảnh)</label>
                            <input type="file" name="imageChildren" multiple class="form-control" onchange="previewImages(this)" />
                            <div id="preview" class="mt-2"></div>
                        </div> *@
                        <div class="form-group">
                            <div class="d-flex align-items-center justify-content-between mb-2">
                                <label class="mb-0">Ảnh mô tả</label>
                                <!-- Nút Chọn nhiều ảnh -->
                                <button type="button" class="btn btn-outline-success btn-sm" onclick="selectMultipleImages()" title="Chọn nhiều ảnh">
                                    <i class="fas fa-images"></i>
                                </button>
                                <!-- Nút Thêm ảnh -->
                                <button type="button" class="btn btn-outline-primary btn-sm" onclick="addImageInput()" title="Thêm ảnh">
                                    <i class="fas fa-plus"></i>
                                </button>
                            </div>

                            <div id="imageChildrenContainer">
                                <!-- JavaScript sẽ tự động thêm input ở đây -->
                            </div>

                            <div id="previewContainer" class="d-flex flex-wrap gap-2 mt-3"></div>
                        </div>
                    </div>
                    <!-- Tab 3: SEO -->
                    <div class="tab-pane fade" id="settings">
                        <div class="form-group">
                            <label asp-for="SeoTitle" class="control-label">Tiêu đề SEO</label>
                            <input asp-for="SeoTitle" class="form-control" />
                            <span asp-validation-for="SeoTitle" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="SeoDescripyion" class="control-label">Mô tả SEO</label>
                            <input asp-for="SeoDescripyion" class="form-control" />
                            <span asp-validation-for="SeoDescripyion" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="SeoKeywords" class="control-label">Từ khóa SEO</label>
                            <input asp-for="SeoKeywords" class="form-control" />
                            <span asp-validation-for="SeoKeywords" class="text-danger"></span>
                        </div>
                    </div>
                </div>

                <div class="mt-3">
                    <a asp-action="Index" class="btn btn-danger">Quay lại</a>
                    <button type="submit" class="btn btn-primary">Thêm mới</button>
                </div>
            </form>
        </div>
        <!-- /.card-body -->
        @* <div class="card-footer">
        Footer
        </div> *@
        <!-- /.card-footer-->
    </div>
    <!-- /.card -->

</section>
<!-- /.content -->
<script>
    document.getElementById('imageUpload').addEventListener('change', function (event) {
        const file = event.target.files[0];
        if (file) {
            // Hiển thị tên tệp đã chọn
            this.nextElementSibling.innerText = file.name;

            // Xem trước ảnh
            const reader = new FileReader();
            reader.onload = function (e) {
                const preview = document.getElementById('previewImage');
                preview.src = e.target.result;
                preview.style.display = 'block';
            };
            reader.readAsDataURL(file);
        }
    });
</script>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script src="/ckeditor/ckeditor.js"></script> <!-- đảm bảo đường dẫn đúng -->

    <script>
        $(document).ready(function () {
            CKEDITOR.replace('txtDetail', {
                customConfig: '/ckeditor/config.js',
                extraAllowedContent: 'span'
            });
        });

        function BrowseServer(field) {
            var finder = new CKFinder();
            finder.selectActionFunction = function (fileUrl) {
                document.getElementById(field).value = fileUrl;
                document.getElementById('previewImage').src = fileUrl;
                document.getElementById('previewImage').style.display = 'block';
            };
            finder.popup();
        }
        // function previewImages(input) {
        //     var preview = document.getElementById("preview");
        //     preview.innerHTML = "";

        //     if (input.files) {
        //         Array.from(input.files).forEach(file => {
        //             var reader = new FileReader();

        //             reader.onload = function (e) {
        //                 var img = document.createElement("img");
        //                 img.src = e.target.result;
        //                 img.style.width = "100px";
        //                 img.style.margin = "5px";
        //                 preview.appendChild(img);
        //             };

        //             reader.readAsDataURL(file);
        //         });
        //     }
        // }
        // Hàm để thêm input ảnh mới (nút Thêm ảnh)
        function addImageInput() {
            var container = document.getElementById("imageChildrenContainer");

            var inputGroup = document.createElement("div");
            inputGroup.className = "input-group mb-2 align-items-center";

            // Tạo div.custom-file
            var customFileDiv = document.createElement("div");
            customFileDiv.className = "custom-file";

            var input = document.createElement("input");
            input.type = "file";
            input.name = "imageChildren"; // Quan trọng!
            input.className = "custom-file-input";
            input.accept = "image/*";

            var label = document.createElement("label");
            label.className = "custom-file-label";
            label.innerText = "Chọn ảnh";

            // Cập nhật tên file khi chọn
            input.onchange = function () {
                if (input.files.length > 0) {
                    label.innerText = input.files[0].name;
                } else {
                    label.innerText = "Chọn ảnh";
                }
                refreshPreview();
            };

            customFileDiv.appendChild(input);
            customFileDiv.appendChild(label);

            var removeBtn = document.createElement("button");
            removeBtn.type = "button";
            removeBtn.className = "btn btn-danger btn-sm ml-2";
            removeBtn.innerHTML = '<i class="fas fa-times"></i>';
            removeBtn.title = "Hủy ảnh này";
            removeBtn.onclick = function () {
                container.removeChild(inputGroup);
                refreshPreview();
            };

            inputGroup.appendChild(customFileDiv);
            inputGroup.appendChild(removeBtn);
            container.appendChild(inputGroup);
        }

        function addImageInputWithFile(file) {
            var container = document.getElementById("imageChildrenContainer");

            var inputGroup = document.createElement("div");
            inputGroup.className = "input-group mb-2 align-items-center";

            var customFileDiv = document.createElement("div");
            customFileDiv.className = "custom-file";

            var input = document.createElement("input");
            input.type = "file";
            input.name = "imageChildren"; // Quan trọng!
            input.className = "custom-file-input";
            input.accept = "image/*";

            const dataTransfer = new DataTransfer();
            dataTransfer.items.add(file);
            input.files = dataTransfer.files;

            var label = document.createElement("label");
            label.className = "custom-file-label";
            label.innerText = file.name;

            input.onchange = function () {
                if (input.files.length > 0) {
                    label.innerText = input.files[0].name;
                } else {
                    label.innerText = "Chọn ảnh";
                }
                refreshPreview();
            };

            customFileDiv.appendChild(input);
            customFileDiv.appendChild(label);

            var removeBtn = document.createElement("button");
            removeBtn.type = "button";
            removeBtn.className = "btn btn-danger btn-sm ml-2";
            removeBtn.innerHTML = '<i class="fas fa-times"></i>';
            removeBtn.title = "Hủy ảnh này";
            removeBtn.onclick = function () {
                container.removeChild(inputGroup);
                refreshPreview();
            };

            inputGroup.appendChild(customFileDiv);
            inputGroup.appendChild(removeBtn);
            container.appendChild(inputGroup);

            showPreview(file);
        }

        function selectMultipleImages() {
            var input = document.createElement("input");
            input.type = "file";
            input.accept = "image/*";
            input.multiple = true;
            input.onchange = function () {
                var files = this.files;
                Array.from(files).forEach(file => {
                    addImageInputWithFile(file);
                });
            };
            input.click();
        }

        function showPreview(file) {
            const reader = new FileReader();
            reader.onload = function (e) {
                const img = document.createElement("img");
                img.src = e.target.result;
                img.style.width = "100px";
                img.style.height = "100px";
                img.style.objectFit = "cover";
                img.style.margin = "5px";
                img.className = "img-thumbnail";
                document.getElementById("previewContainer").appendChild(img);
            };
            reader.readAsDataURL(file);
        }

        function refreshPreview() {
            var previewContainer = document.getElementById("previewContainer");
            previewContainer.innerHTML = "";

            const inputs = document.querySelectorAll('input[type="file"][name="imageChildren"]');
            inputs.forEach(input => {
                if (input.files && input.files[0]) {
                    showPreview(input.files[0]);
                }
            });
        }
    </script>
}

